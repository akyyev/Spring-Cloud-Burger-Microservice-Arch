spring:
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        serviceId: config
  eureka:
    client:
      registeredWithEureka: true
      fetchRegistry: true
      serviceUrl:
        defaultZone: http://localhost:8761/eureka

